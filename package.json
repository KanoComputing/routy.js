{
  "name": "routy",
  "version": "0.1.1",
  "description": "A dead simple module to manage hash routing in the browser.",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p build; browserify lib/browser.js -o build/Routy.js; uglifyjs build/Routy.js -o build/Routy.min.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "uglify-js": "~2.4.x",
    "browserify": "~3.32.x"
  },
  "dependencies": {
    "route-pattern": "0.0.x",
    "tiny-emitter": "~0.1.x"
  }
}
